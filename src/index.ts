function greet(name: string): string  {
    return `hello ${name}`
 }
 console.log(greet("world"))


 // even u can tyoecast the variable
 const username : string = "param"
 console.log(greet(username))